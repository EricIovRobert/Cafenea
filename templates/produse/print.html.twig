<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Lista Produse</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: center;
        }

        th {
            background-color: #f2f2f2;
        }

        h1 {
            text-align: center;
            margin-bottom: 5px;
        }

        p {
            text-align: center;
            margin: 0;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <h1>Lista Produse pentru data {{ current_date }} ({{current_day}})</h1>


    <table>
        <thead>
            <tr>
                <th>Nr.</th>
                <th>Nume</th>
                <th>Stoc</th>
                <th>Observatii</th>
            </tr>
        </thead>
        <tbody>
            {% for produs in produse %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td>{{ produs.Nume }}</td>
                    <td>{{ produs.Stoc }}</td>
                    <td>{{ produs.Observatii }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
