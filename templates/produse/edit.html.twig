{% extends 'base.html.twig' %}

{% block title %}Editează Produs{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1>Editează Produs</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success mt-3">Salvează</button>
        <a href="{{ path('produse') }}" class="btn btn-secondary mt-3">Înapoi la listă</a>
    {{ form_end(form) }}
</div>
{% endblock %}
